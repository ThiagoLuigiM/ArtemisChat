# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri / Rust build output
src-tauri/target/
src-tauri/gen/schemas/
# Cargo.lock é commitado (app, não lib — reprodutibilidade no CI)

# Tauri updater — chave PRIVADA jamais é commitada (a pública vai inline no tauri.conf.json)
*.key
*.key.tmp
.tauri/

# Secrets / config local
*.env
.env.local
.env.*.local

# OS
Thumbs.db

# Build artifacts / releases baixadas localmente para teste
*.msi
*.msi.sig
ArtemisChat-*.zip
Artemis_*.zip
latest.json
